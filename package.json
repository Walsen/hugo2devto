{
  "name": "hugo-to-devto-action",
  "version": "1.2.0",
  "description": "GitHub Action to publish Hugo blog posts to dev.to",
  "main": "dist/index.js",
  "scripts": {
    "build": "ncc build src/index.ts -o dist --source-map --license licenses.txt",
    "package": "npm run build",
    "test": "node --import tsx --test test/*.test.ts"
  },
  "keywords": [
    "github-action",
    "devto",
    "hugo",
    "blog",
    "publish"
  ],
  "author": "",
  "license": "MIT",
  "dependencies": {
    "@actions/core": "^1.10.1"
  },
  "devDependencies": {
    "@types/node": "^20.11.0",
    "@vercel/ncc": "^0.38.1",
    "tsx": "^4.19.2",
    "typescript": "^5.3.3"
  }
}
